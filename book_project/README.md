# 📘 Book1 – Django Book Management App

A simple and clean **Django Book Management Application** built to practice Django models, views, templates, and full CRUD operations.  
This project allows users to **add, view, edit, and delete books** easily through a user-friendly interface.

---

## 🚀 Features

- ➕ Add new books with details  
- ✏️ Edit and update existing book records  
- ❌ Delete books from the list  
- 📄 Display all books in a clean, organized layout  
- 🛠️ Built using Django models, views, URLs, and templates  
- 🔧 Fully functional CRUD (Create, Read, Update, Delete)

---

## 🧰 Prerequisites

Before running this project, ensure you have:

- **Python 3.x**  
- **Django framework** installed  
  ```sh
  pip install django


📦 Installation & Setup

Follow these steps to run the project locally:

1. Clone the repository
git clone https://github.com/TILAKMK/book1.git

2. Navigate into the project folder
cd book1

3. Create and activate a virtual environment (Windows)
python -m venv env
env\Scripts\activate

4. Install dependencies
pip install -r requirements.txt


If you don’t have a requirements.txt yet:

pip freeze > requirements.txt

5. Run migrations
python manage.py migrate

6. Start the development server
python manage.py runserver

7. Open the app in your browser
http://127.0.0.1:8000/